---
description: Synkra AIOS Development Rules - Core framework rules for AI-Orchestrated Full Stack Development
globs:
alwaysApply: true
---

# Synkra AIOS Development Rules for Cursor

You are working with Synkra AIOS, an AI-Orchestrated System for Full Stack Development.

## Core Development Rules

### Agent Integration
- Recognize AIOS agent activations: @dev, @qa, @architect, @pm, @po, @sm, @analyst, @aios-master
- Agent commands use * prefix: *help, *create-story, *task, *exit
- Follow agent-specific workflows and patterns
- When user types @{agent-name}, load the corresponding agent file from `.antigravity/agents/{agent-name}.md` and adopt that persona completely

### Story-Driven Development
1. **Always work from a story file** in docs/stories/
2. **Update story checkboxes** as you complete tasks: [ ] → [x]
3. **Maintain the File List** section with all created/modified files
4. **Follow acceptance criteria** exactly as written

### Code Quality Standards
- Write clean, maintainable code following project conventions
- Include comprehensive error handling
- Add unit tests for all new functionality
- Follow existing patterns in the codebase

### Testing Protocol
- Run all tests before marking tasks complete
- Ensure linting passes: `npm run lint`
- Verify type checking: `npm run typecheck`
- Add tests for new features

## AIOS Framework Structure

```
.aios-core/
├── core/            # Core engine modules
├── development/     # Agent definitions, tasks, workflows, templates
├── infrastructure/  # Scripts, tools, utilities
├── product/         # Templates, checklists, best practices
├── data/            # Knowledge base and patterns
├── cli/             # CLI commands
└── core-config.yaml # Project configuration

.antigravity/
├── agents/          # Agent persona definitions
└── rules.md         # Global rules

docs/
├── stories/         # Development stories
├── prd/             # Sharded PRD sections
└── architecture/    # Sharded architecture
```

## Development Workflow

1. **Read the story** - Understand requirements fully
2. **Implement sequentially** - Follow task order
3. **Test thoroughly** - Validate each step
4. **Update story** - Mark completed items
5. **Document changes** - Update File List

## Best Practices

### When implementing:
- Check existing patterns first
- Reuse components and utilities
- Follow naming conventions
- Keep functions focused and small

### When testing:
- Write tests alongside implementation
- Test edge cases
- Verify error handling
- Run full test suite

## Git & GitHub
- Use conventional commits: `feat:`, `fix:`, `docs:`, etc.
- Reference story ID in commits: `feat: implement IDE detection [Story 2.1]`
- Push regularly to avoid conflicts

## IDE-FILE-RESOLUTION
- Dependencies map to `.aios-core/development/{type}/{name}`
- type=folder (tasks|templates|checklists|data|utils|etc...), name=file-name
- Example: create-doc.md -> `.aios-core/development/tasks/create-doc.md`
- Only load these files when user requests specific command execution
